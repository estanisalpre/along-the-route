# Por ahora el ruteo usa el servidor demo público de OSRM (gratis, sin API
# key, ver src/map/routing/osrmProvider.ts) — no hace falta ninguna variable
# acá. Se cambió desde OpenRouteService porque la clave gratuita se queda sin
# cuota fácil, y el navegador reporta ese error como si fuera CORS.
